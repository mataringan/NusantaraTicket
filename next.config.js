/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com", "cdn.pixabay.com", "flowbite.com"],
    },
};

module.exports = nextConfig;

//test
